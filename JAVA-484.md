# Final Java Practice Questions — Basics Only

**Version:** Revised list without arrays and without String-operation questions.

**Total questions:** 441

**Kept:** `String` only as a basic data type for storing, displaying, formatting, parsing, and text input.

**Removed from the earlier 460-question list:** 19 questions based on arrays or String operations/comparisons.

## Topic Summary

| Topic | Easy | Medium | Hard | Total |
|---|---:|---:|---:|---:|
| Output and Print Statements | 7 | 3 | 0 | 10 |
| Variables, Data Types and Constants | 10 | 11 | 0 | 21 |
| Input Handling with Scanner and BufferedReader | 11 | 5 | 5 | 21 |
| Formatted Output with printf() | 4 | 4 | 2 | 10 |
| Parsing, Casting, Type Promotion and Wrapper Classes | 8 | 12 | 10 | 30 |
| Arithmetic and Assignment Operators | 10 | 9 | 1 | 20 |
| Relational, Logical and Unary Operators | 9 | 10 | 10 | 29 |
| Ternary and Bitwise Operators | 7 | 3 | 10 | 20 |
| Operator Precedence, Associativity and Complex Expressions | 2 | 6 | 2 | 10 |
| Formula-Based Programs | 6 | 8 | 6 | 20 |
| If, Else-if and Nested Conditionals | 20 | 15 | 5 | 40 |
| Switch, Enhanced Switch and Ternary Decisions | 10 | 20 | 10 | 40 |
| For, While and Do-while Loops | 15 | 10 | 10 | 35 |
| Break, Continue and Infinite Loops | 0 | 10 | 5 | 15 |
| Input-Controlled Loops and Counting Problems | 10 | 7 | 3 | 20 |
| Number Logic, Series and Conversion Problems | 4 | 7 | 4 | 15 |
| Nested Loops and Patterns | 10 | 12 | 3 | 25 |
| Simple Void Methods | 10 | 7 | 3 | 20 |
| Parameter Methods and Return Methods | 10 | 10 | 10 | 30 |
| Advanced Methods and Method-Based Programs | 6 | 3 | 1 | 10 |

---

## Output and Print Statements

### Easy

1. Your teacher asks you to display your first Java output. Create a program that prints Hello World on the screen.

2. Create a program that displays your name on the screen.

3. Create a program that displays your age on the screen.

4. Create a program that displays your name, age, and city on three different lines.

5. Create a program that displays your name, age, and marks in a single line.

6. Create a program that prints two messages on the same line using System.out.print().

7. Create a program that prints two messages on separate lines using System.out.println().

### Medium

8. Create a program that displays a short student introduction using multiple print statements.

9. Create a program that displays name, roll number, and branch with proper spacing using tab space.

10. Create a program that displays a short paragraph using newline characters.

## Variables, Data Types and Constants

### Easy

11. A student has a roll number. Store the roll number in an integer variable and display it.

12. A small number has to be stored using byte. Store any valid byte value and display it.

13. A school code has to be stored using short. Store any short value and display it.

14. A person's age has to be stored using int. Store the age and display it.

15. A country's population has to be stored using long. Store a large number and display it.

16. A product rating has to be stored using float. Store a rating like 4.5 and display it.

17. A student's percentage has to be stored using double. Store the percentage and display it.

18. A student grade has to be stored using char. Store a grade like A and display it.

19. A student pass status has to be stored using boolean. Store true or false and display it.

20. A student name has to be stored using String. Store the name and display it.

### Medium

21. Create a program that stores your name, age, grade, and percentage using suitable data types and displays them.

22. Create a program that declares a variable first, assigns a value later, and then prints it.

23. Create a program that initializes a variable at the time of declaration and prints it.

24. Create a program that changes the value of a variable and prints the old and new values.

25. A college name should not change in the program. Store it using final and display it.

26. Create a program that stores the maximum marks of an exam using final and displays it.

27. Create a program that stores a student's name, roll number, marks, grade, and result status.

28. Create a program that stores product name, price, quantity, and availability status.

29. Create a program that stores employee ID, salary, department code, and working status.

30. Create a program that stores your personal details using correct primitive and reference data types.

31. A student name is given as a String. Store it and display it.

## Input Handling with Scanner and BufferedReader

### Easy

32. Create a Scanner object and use it to read a number from the user.

33. Ask the user to enter an integer number and display the same number.

34. Ask the user to enter a byte value and display it.

35. Ask the user to enter a short value and display it.

36. Ask the user to enter a long value and display it.

37. Ask the user to enter a float value and display it.

38. Ask the user to enter a double value and display it.

39. Ask the user to enter a character and display it.

40. Ask the user to enter true or false and display the boolean value.

41. Ask the user to enter one word and display it.

42. Ask the user to enter a full sentence and display it using nextLine().

### Medium

43. Ask the user to enter their name and display a greeting message.

44. Ask the user to enter their age and display it with a message.

45. Ask the user to enter their name and age, then display both in one sentence.

46. Ask the user to enter an integer and a decimal number, then display both.

47. Ask the user to enter two integers and display their sum.

### Hard

48. Ask the user to enter their name using BufferedReader and display it.

49. Ask the user to enter an integer using BufferedReader and display it.

50. Ask the user to enter a double value using BufferedReader and display it.

51. Ask the user to enter two numbers using BufferedReader and display their sum.

52. Ask the user to enter a full sentence using BufferedReader and display it.

## Formatted Output with printf()

### Easy

53. A student roll number is stored as an integer. Display it using %d.

54. A product price is stored as a decimal value. Display it using %f.

55. A student name is stored as a String. Display it using %s.

56. A grade is stored as a character. Display it using %c.

### Medium

57. A percentage value contains many decimal digits. Display it up to 2 decimal places.

58. Store a student's name, age, and percentage. Display them using printf().

59. Store product name, price, and quantity. Display them in a formatted bill.

60. Store marks obtained and total marks. Display the percentage using precision formatting.

### Hard

61. Display name, roll number, and marks in a neat table-like format using printf().

62. Create a simple student report card using formatted output.

## Parsing, Casting, Type Promotion and Wrapper Classes

### Easy

63. A number is stored as the String "123". Convert it into an integer and display it.

64. A large number is stored as a String. Convert it into long and display it.

65. A decimal number is stored as the String "45.67". Convert it into double and display it.

66. A boolean value is stored as the String "true". Convert it into boolean and display it.

67. An integer value is given. Convert it into String and display it.

68. A double value is given. Convert it into String and display it.

69. A character is given. Convert it into integer and display its ASCII value.

70. Unicode value 65 is given. Store it in a char variable and display the character.

### Medium

71. An int value is given. Convert it into long and display both values.

72. An int value is given. Convert it into double and display both values.

73. A float value is given. Convert it into double and display both values.

74. A double value is given. Convert it into int and observe the output.

75. A long value is given. Convert it into int and display the result.

76. An int value is given. Convert it into char and display the character.

77. Demonstrate implicit casting by converting a smaller data type into a larger data type.

78. Demonstrate explicit casting by converting a larger data type into a smaller data type.

79. Add two byte values and show how type promotion happens.

80. Add a char value and an int value and display the result.

81. Add an int value and a double value and display the promoted result.

82. Create one arithmetic expression using different data types and display the final result.

### Hard

83. Store an int value inside an Integer wrapper object and display it.

84. Store a long value inside a Long wrapper object and display it.

85. Store a double value inside a Double wrapper object and display it.

86. Store a float value inside a Float wrapper object and display it.

87. Store a char value inside a Character wrapper object and display it.

88. Store a boolean value inside a Boolean wrapper object and display it.

89. Store a byte value inside a Byte wrapper object and display it.

90. Store a short value inside a Short wrapper object and display it.

91. Assign an int value directly to an Integer object and demonstrate autoboxing.

92. Assign an Integer object value to an int variable and demonstrate unboxing.

## Arithmetic and Assignment Operators

### Easy

93. Two numbers are given by the user. Find and display their sum.

94. Two numbers are given by the user. Find and display their difference.

95. Two numbers are given by the user. Find and display their product.

96. Two numbers are given by the user. Find and display their division result.

97. Two numbers are given by the user. Find and display the remainder.

98. A dividend and divisor are given. Display both quotient and remainder.

99. A number is given by the user. Extract and display its last digit.

100. A number is given by the user. Extract and display its last two digits.

101. A number is given by the user. Find and display its square.

102. A number is given by the user. Find and display its cube.

### Medium

103. Create a variable, assign a value using =, and display it.

104. A wallet has an initial balance. Add money using += and display the updated balance.

105. A wallet has an initial balance. Spend some money using -= and display the updated balance.

106. A number is given. Multiply it using *= and display the updated value.

107. A number is given. Divide it using /= and display the updated value.

108. A number is given. Find its updated remainder using %= and display it.

109. A student has marks in one subject. Add marks of another subject using +=.

110. A salary is given. Increase it using an assignment operator and display the new salary.

111. A stock quantity is given. Reduce it using an assignment operator and display the remaining stock.

### Hard

112. A value is given. Apply multiple assignment operators one by one and display the result after each step.

## Relational, Logical and Unary Operators

### Easy

113. Two integers are given. Check whether both are equal.

114. Two integers are given. Check whether both are not equal.

115. Two integers are given. Check whether the first number is greater than the second.

116. Two integers are given. Check whether the first number is smaller than the second.

117. Two integers are given. Check whether the first number is greater than or equal to the second.

118. Two integers are given. Check whether the first number is less than or equal to the second.

119. Two decimal numbers are given. Compare them and display the result.

120. Two characters are given. Compare their ASCII values and display the result.

121. Create a program that displays the result of at least five relational expressions.

### Medium

122. A student has marks in two subjects. Check whether the student passed both subjects using &&.

123. A person has either an Aadhaar card or passport. Check eligibility using ||.

124. A boolean value is given. Reverse it using logical NOT.

125. A number is given. Check whether it is greater than 10 and less than 100.

126. A number is given. Check whether it is less than 0 or greater than 100.

127. A character is given. Check whether it is an uppercase letter using logical operators.

128. A character is given. Check whether it is a lowercase letter using logical operators.

129. A character is given. Check whether it is an alphabet using logical operators.

130. A character is given. Check whether it is a digit using logical operators.

131. Create a program that demonstrates short-circuit behavior of && and ||.

### Hard

132. A number is given. Apply pre-increment and display the result.

133. A number is given. Apply post-increment and display the result before and after increment.

134. A number is given. Apply pre-decrement and display the result.

135. A number is given. Apply post-decrement and display the result before and after decrement.

136. A positive number is given. Use unary plus and display the result.

137. A positive number is given. Use unary minus and display the result.

138. A positive temperature value is given. Convert it into negative using unary minus.

139. A negative number is given. Convert it into positive and display the result.

140. Create a program that clearly shows the difference between ++a and a++.

141. Create a program that clearly shows the difference between --a and a--.

## Ternary and Bitwise Operators

### Easy

142. Two numbers are given. Find the greater number using the ternary operator.

143. Two numbers are given. Find the smaller number using the ternary operator.

144. A number is given. Check whether it is positive or negative using the ternary operator.

145. A number is given. Check whether it is even or odd using the ternary operator.

146. A number is given. Check whether it is divisible by 5 using the ternary operator.

147. A number is given. Check whether it is divisible by both 3 and 5 using the ternary operator.

148. A person's age is given. Check whether the person is eligible to vote using the ternary operator.

### Medium

149. Three numbers are given. Find the maximum number using the ternary operator.

150. Three numbers are given. Find the minimum number using the ternary operator.

151. A student's marks are given. Display Pass or Fail using the ternary operator.

### Hard

152. Two integer values are given. Apply bitwise AND and display the result.

153. Two integer values are given. Apply bitwise OR and display the result.

154. Two integer values are given. Apply bitwise XOR and display the result.

155. One integer value is given. Apply bitwise NOT and display the result.

156. A number is given. Apply left shift by 1 and display the result.

157. A number is given. Apply right shift by 1 and display the result.

158. A number is given. Apply unsigned right shift and display the result.

159. A number is given. Check whether it is even or odd using bitwise AND.

160. A number is given. Multiply it by 2 using the left shift operator.

161. A number is given. Divide it by 2 using the right shift operator.

## Operator Precedence, Associativity and Complex Expressions

### Easy

162. Evaluate the expression 10 + 5 * 2 in Java and display the result.

163. Evaluate the expression (10 + 5) * 2 in Java and display the result.

### Medium

164. Create a program to show which operator executes first between multiplication and division.

165. Create a program to show which operator executes first between addition and relational operators.

166. Create a program to show which operator executes first between relational and logical operators.

167. Create a program to show the precedence difference between && and ||.

168. Create a program to show assignment operator associativity using multiple variables.

169. Create a program to show arithmetic operator associativity using subtraction or division.

### Hard

170. Create a complex expression using arithmetic, relational, and logical operators, then display the result.

171. Rewrite a complex expression using brackets and compare both outputs.

## Formula-Based Programs

### Easy

172. A rectangle has length and breadth. Take both as input and calculate its area.

173. A rectangle has length and breadth. Take both as input and calculate its perimeter.

174. A circle has a radius. Take radius as input and calculate its area.

175. A circle has a radius. Take radius as input and calculate its circumference.

176. Temperature is given in Celsius. Convert it into Fahrenheit.

177. Temperature is given in Fahrenheit. Convert it into Celsius.

### Medium

178. Principal, rate, and time are given. Calculate simple interest.

179. Principal, rate, and time are given. Calculate compound interest.

180. Three subject marks are given. Calculate the average marks.

181. Marks of five subjects are given. Calculate total marks and percentage.

182. A number of days is given. Convert it into years, months, and remaining days.

183. A number of minutes is given. Convert it into hours and remaining minutes.

184. Weight and height are given. Calculate BMI.

185. An employee's salary and bonus percentage are given. Calculate the final salary.

### Hard

186. Cost price and selling price are given. Calculate profit or loss amount.

187. Cost price and selling price are given. Calculate profit or loss percentage.

188. Units consumed are given. Calculate a simple electricity bill using a fixed rate per unit.

189. Two numbers are given. Swap their values using a third variable.

190. Two numbers are given. Swap their values without using a third variable.

191. Two very large numbers are given. Multiply them safely using long and display the result.

## If, Else-if and Nested Conditionals

### Easy

192. A number is entered by the user. Check whether the number is positive.

193. A student's marks are entered. Check whether the student has passed if marks are 35 or above.

194. A person's age is entered. Check whether the person is eligible to vote.

195. A number is entered. Check whether it is divisible by 5.

196. A number is entered. Check whether it is an even number.

197. A character is entered. Check whether it is an uppercase letter.

198. A character is entered. Check whether it is a lowercase letter.

199. A number is entered. Check whether it is greater than 100.

200. A year is entered. Check whether it is a century year.

201. A product price is entered. Check whether discount is applicable if price is above 1000.

202. A number is entered. Check whether it is positive or negative.

203. A number is entered. Check whether it is even or odd.

204. Two numbers are entered. Find the greater number.

205. Two numbers are entered. Find the smaller number.

206. A student's marks are entered. Display Pass or Fail.

207. A person's age is entered. Check whether the person is eligible for a driving license.

208. A number is entered. Find its absolute value.

209. A character is entered. Check whether it is a vowel or consonant.

210. A number is entered. Check whether it is divisible by both 3 and 5.

211. A selling price and cost price are entered. Check whether there is profit or loss.

### Medium

212. A number is entered. Check whether it is positive, negative, or zero.

213. Marks are entered. Print grade using if-else-if.

214. A character is entered. Check whether it is an alphabet, digit, or special character.

215. Three numbers are entered. Find the greatest number.

216. Three numbers are entered. Find the smallest number.

217. Four numbers are entered. Find the largest number.

218. A percentage is entered. Display distinction, first class, second class, pass, or fail.

219. A month number is entered. Display the number of days in that month.

220. An electricity unit value is entered. Calculate electricity bill using slabs.

221. An income value is entered. Calculate income tax using slabs.

222. Three sides of a triangle are entered. Check whether the triangle is valid.

223. Three sides of a triangle are entered. Check whether it is equilateral.

224. Three sides of a triangle are entered. Check whether it is isosceles.

225. Three sides of a triangle are entered. Check whether it is scalene.

226. Three angles are entered. Check whether they form a valid triangle.

### Hard

227. A year is entered. Check whether it is a leap year using nested if.

228. A username and password are entered. Build a simple login authentication system.

229. A balance and withdrawal amount are entered. Build a simple ATM withdrawal simulation.

230. A student's marks and attendance are entered. Check scholarship eligibility.

231. An employee's salary and experience are entered. Decide bonus eligibility using nested if.

## Switch, Enhanced Switch and Ternary Decisions

### Easy

232. A number from 1 to 7 is entered. Print the weekday name using switch.

233. A number from 1 to 12 is entered. Print the month name using switch.

234. Two numbers and an operator are entered. Create a simple calculator using switch.

235. A menu choice is entered. Perform addition, subtraction, multiplication, or division using switch.

236. A character is entered. Check whether it is a vowel using switch.

237. A grade is entered. Print a grade message using switch.

238. A recharge plan number is entered. Display the selected mobile recharge plan using switch.

239. A restaurant menu choice is entered. Display the selected food item and price using switch.

240. A movie ticket type is entered. Calculate ticket price using switch.

241. A course code is entered. Display the course name using switch.

### Medium

242. A day number is entered. Print the day name using enhanced switch.

243. A month number is entered. Print the month name using enhanced switch.

244. A grade is entered. Print performance message using enhanced switch.

245. A calculator operator is entered. Perform the operation using enhanced switch.

246. A traffic light color is entered. Print the correct instruction using enhanced switch.

247. A food menu option is entered. Display item price using enhanced switch.

248. A recharge plan option is entered. Display plan details using enhanced switch.

249. A role name is entered. Display access level using enhanced switch.

250. A number from 1 to 4 is entered. Display season name using enhanced switch.

251. A character is entered. Check whether it is a vowel using enhanced switch.

252. Create a switch program without break statements and observe fall-through behavior.

253. Create a switch program with break statements and compare it with fall-through behavior.

254. A number from 1 to 3 is entered. Use switch without break and observe multiple cases executing.

255. A number from 1 to 3 is entered. Use switch with break and display only the matching case.

256. Create a weekday/weekend checker using switch and grouped cases.

257. Create a month-days program using switch and grouped cases.

258. Create a vowel checker using switch and grouped cases.

259. Create a simple calculator using switch and break after every case.

260. Create a menu-driven program using switch and default case for invalid choice.

261. Create a switch program that demonstrates the use of default.

### Hard

262. A number is entered. Check whether it is even or odd using the ternary operator.

263. A number is entered. Check whether it is positive or negative using the ternary operator.

264. Two numbers are entered. Find the greater number using the ternary operator.

265. Two numbers are entered. Find the smaller number using the ternary operator.

266. A person's age is entered. Check voting eligibility using the ternary operator.

267. Marks are entered. Display Pass or Fail using the ternary operator.

268. A number is entered. Check whether it is divisible by 5 using the ternary operator.

269. Three numbers are entered. Find the greatest number using nested ternary operators.

270. Three numbers are entered. Find the smallest number using nested ternary operators.

271. A character is entered. Check whether it is uppercase or lowercase using the ternary operator.

## For, While and Do-while Loops

### Easy

272. Print numbers from 1 to 10 using a for loop.

273. Print numbers from 10 to 1 using a for loop.

274. Print even numbers from 1 to 100 using a for loop.

275. Print odd numbers from 1 to 100 using a for loop.

276. Print the multiplication table of a number entered by the user.

277. Print multiplication tables from 1 to 10.

278. Find the sum of first n natural numbers.

279. Find the factorial of a number.

280. Find the power of a number using a loop.

281. Print squares of numbers from 1 to 20.

282. Print cubes of numbers from 1 to 20.

283. Print all numbers divisible by 3 from 1 to n.

284. Print all numbers divisible by 5 from 1 to n.

285. Count how many even numbers exist between 1 and n.

286. Count how many odd numbers exist between 1 and n.

### Medium

287. Count the digits of a number using a while loop.

288. Reverse the digits of a number using a while loop.

289. Find the sum of digits of a number using a while loop.

290. Find the product of digits of a number using a while loop.

291. Check whether a number is a palindrome using a while loop.

292. Find factorial of a number using a while loop.

293. Print Fibonacci series up to n terms using a while loop.

294. Find the GCD of two numbers using a while loop.

295. Find the LCM of two numbers using a while loop.

296. Keep taking input from the user until the user enters -1.

### Hard

297. Print numbers from 1 to 10 using a do-while loop.

298. Ask the user to enter a number and print it at least once using a do-while loop.

299. Create a menu-driven calculator using a do-while loop.

300. Create a menu that keeps showing until the user chooses Exit.

301. Keep asking the user for a password until the correct password is entered.

302. Keep asking the user for a PIN until the correct PIN is entered.

303. Create a banking menu with deposit, withdraw, check balance, and exit options.

304. Keep asking the user to enter positive numbers until a negative number is entered.

305. Keep taking marks input until the user enters -1, then display total marks.

306. Create a program that asks the user whether they want to continue after each operation.

## Break, Continue and Infinite Loops

### Medium

307. Print numbers from 1 to 20, but stop the loop when the number becomes 11.

308. Print numbers from 1 to 20, but skip the number 13.

309. Take numbers from the user and stop when the user enters 0.

310. Search for a number in a fixed list and stop when it is found.

311. Print numbers from 1 to 50, but skip multiples of 5.

312. Print odd numbers from 1 to 30 using continue.

313. Print even numbers from 1 to 30 using continue.

314. Keep taking marks input and stop if invalid marks are entered.

315. Create a loop that stops when a correct password is entered.

316. Create a loop that skips negative numbers and adds only positive numbers.

### Hard

317. Create an infinite loop using while true and stop it using break.

318. Create an infinite menu program that stops only when the user chooses Exit.

319. Create an infinite calculator menu that stops only when the user enters 0.

320. Create an infinite input program that keeps taking numbers until -1 is entered.

321. Create an infinite loop that prints numbers and stops after a condition becomes true.

## Input-Controlled Loops and Counting Problems

### Easy

322. Keep taking numbers from the user until -1 is entered, then display the sum.

323. Keep taking marks from the user until -1 is entered, then display the average.

324. Keep taking product prices until 0 is entered, then display the total bill.

325. Keep taking integers until 0 is entered, then count positive and negative numbers.

326. Keep taking numbers until -999 is entered, then display the largest number.

327. Keep taking numbers until -999 is entered, then display the smallest number.

328. Keep taking numbers until 0 is entered, then display the sum of even numbers.

329. Keep taking numbers until 0 is entered, then display the sum of odd numbers.

330. Keep taking characters until # is entered, then count vowels.

331. Keep taking numbers until -1 is entered, then display how many numbers were entered.

### Medium

332. Ask the user how many numbers they want to enter, then calculate their sum.

333. Ask the user how many marks they want to enter, then calculate the average.

334. Ask the user how many products they bought, then calculate the total bill.

335. Ask the user how many employees are there, then calculate total salary.

336. Ask the user for n and print n natural numbers.

337. Ask the user for n and calculate factorial using exactly n loop steps.

338. Ask the user for n and print n terms of the Fibonacci series.

### Hard

339. Ask the user how many students are in a class, then count pass and fail students.

340. Ask the user how many numbers they want to enter, then find the greatest number.

341. Ask the user how many numbers they want to enter, then find the smallest number.

## Number Logic, Series and Conversion Problems

### Easy

342. A number is entered. Check whether it is a prime number.

343. A limit n is entered. Print all prime numbers from 1 to n.

344. A number is entered. Find all factors of that number.

345. A number is entered. Count the number of factors.

### Medium

346. A number is entered. Check whether it is a perfect number.

347. A number is entered. Check whether it is an Armstrong number.

348. A number is entered. Check whether it is a strong number.

349. A number is entered. Find the sum of factorials of its digits.

350. A decimal number is entered. Convert it into binary.

351. A binary number is entered. Convert it into decimal.

352. A position n is entered. Find the nth Fibonacci number.

### Hard

353. Create a number guessing game where the user keeps guessing until the correct number is entered.

354. Print multiplication tables repeatedly until the user chooses to exit.

355. Build a password validation system using loops and conditions.

356. Build a PIN verification system using loops and conditions.

## Nested Loops and Patterns

### Easy

357. Print an increasing star triangle.

358. Print a decreasing star triangle.

359. Print a number increasing triangle.

360. Print a repeated number triangle.

361. Print a character increasing triangle.

362. Print a solid square star pattern.

363. Print a solid square number pattern.

364. Print a continuous number triangle.

365. Print a binary triangle pattern.

366. Print a repeated character triangle.

### Medium

367. Print a full pyramid pattern.

368. Print an inverted pyramid pattern.

369. Print a hollow square pattern.

370. Print a hollow rectangle pattern.

371. Print a right-aligned triangle.

372. Print a left-aligned triangle.

373. Print a diamond pattern.

374. Print Floyd's triangle.

375. Print Pascal's triangle.

376. Print a number pyramid.

377. Print a reverse number triangle.

378. Print a character pyramid.

### Hard

379. Print a hollow pyramid.

380. Print a butterfly pattern.

381. Print a palindrome pyramid pattern.

## Simple Void Methods

### Easy

382. Create a method named printWelcome that prints a welcome message and call it from main.

383. Create a method named printJava that prints "I am learning Java" and call it from main.

384. Create a method named printDetails that prints your name, age, and city.

385. Create a method named printLine that prints a line of dashes.

386. Create a method named printMenu that displays a simple calculator menu.

387. Create a method named printNumbers that prints numbers from 1 to 10.

388. Create a method named printEvenNumbers that prints even numbers from 1 to 20.

389. Create a method named printOddNumbers that prints odd numbers from 1 to 20.

390. Create a method named printSquareNumbers that prints squares from 1 to 10.

391. Create a method named printCubeNumbers that prints cubes from 1 to 10.

### Medium

392. Create a void method named greetUser that takes no parameters and prints a greeting.

393. Create a void method named showCollegeName that prints a fixed college name.

394. Create a void method named showRules that prints three simple classroom rules.

395. Create a void method named showResultMessage that prints "Result will be announced soon".

396. Create a void method named displaySeparator that prints a separator line.

397. Create a void method named showProgramStart that prints a program start message.

398. Create a void method named showProgramEnd that prints a program end message.

### Hard

399. Create a void method named printTableOfFive that prints the multiplication table of 5.

400. Create a void method named printFirstTenNaturalNumbers that prints numbers 1 to 10.

401. Create a void method named printFirstTenNaturalNumbersReverse that prints numbers 10 to 1.

## Parameter Methods and Return Methods

### Easy

402. Create a method named addTwoNumbers that takes two integers as parameters and prints their sum.

403. Create a method named subtractTwoNumbers that takes two integers as parameters and prints their difference.

404. Create a method named multiplyTwoNumbers that takes two integers as parameters and prints their product.

405. Create a method named divideTwoNumbers that takes two integers as parameters and prints their division result.

406. Create a method named findRemainder that takes two integers as parameters and prints the remainder.

407. Create a method named printTable that takes one integer as a parameter and prints its multiplication table.

408. Create a method named printRange that takes two integers as parameters and prints numbers between them.

409. Create a method named printSquare that takes one integer as a parameter and prints its square.

410. Create a method named printCube that takes one integer as a parameter and prints its cube.

411. Create a method named printPower that takes base and exponent as parameters and prints the power result.

### Medium

412. Create a method named add that takes two integers and returns their sum.

413. Create a method named subtract that takes two integers and returns their difference.

414. Create a method named multiply that takes two integers and returns their product.

415. Create a method named divide that takes two double values and returns the division result.

416. Create a method named square that takes one integer and returns its square.

417. Create a method named cube that takes one integer and returns its cube.

418. Create a method named areaOfRectangle that takes length and breadth and returns the area.

419. Create a method named perimeterOfRectangle that takes length and breadth and returns the perimeter.

420. Create a method named areaOfCircle that takes radius and returns the area.

421. Create a method named simpleInterest that takes principal, rate, and time and returns simple interest.

### Hard

422. Create a method named isEven that takes an integer and returns true if it is even.

423. Create a method named isOdd that takes an integer and returns true if it is odd.

424. Create a method named isPositive that takes an integer and returns true if it is positive.

425. Create a method named isNegative that takes an integer and returns true if it is negative.

426. Create a method named isDivisibleByFive that takes an integer and returns true if it is divisible by 5.

427. Create a method named maxOfTwo that takes two integers and returns the greater number.

428. Create a method named minOfTwo that takes two integers and returns the smaller number.

429. Create a method named maxOfThree that takes three integers and returns the greatest number.

430. Create a method named minOfThree that takes three integers and returns the smallest number.

431. Create a method named passOrFail that takes marks and returns true if marks are 35 or above.

## Advanced Methods and Method-Based Programs

### Easy

432. Create a method named factorial that takes an integer and returns its factorial.

433. Create a method named sumOfNaturalNumbers that takes n and returns the sum of first n natural numbers.

434. Create a method named countDigits that takes an integer and returns the number of digits.

435. Create a method named reverseNumber that takes an integer and returns the reversed number.

436. Create a method named sumOfDigits that takes an integer and returns the sum of its digits.

437. Create a method named productOfDigits that takes an integer and returns the product of its digits.

### Medium

438. Create a method named isPalindrome that takes an integer and returns true if it is a palindrome.

439. Create a method named isPrime that takes an integer and returns true if it is prime.

440. Create overloaded methods named add: one method should add two integers, and another method should add three integers.

### Hard

441. Create a menu-driven calculator where addition, subtraction, multiplication, division, and remainder are written as separate methods.
